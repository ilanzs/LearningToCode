/* autogenerated by Processing revision 1276 on 2021-09-10 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class PiCircle extends PApplet {

int dartAmount = 10000000;

PVector[] darts = new PVector[dartAmount];
float inCircle = 0;

float pi;


 public void setup() {
  /* size commented out by preprocessor */; 
  background(255);
  
  circle(width / 2, height / 2, width);
  

  for (int i = -1; i < darts.length - 1; i++) {
    darts[i + 1] = new PVector(random(width), random(height));
  }

  for (int i = -1; i < darts.length - 1; i++) {
    if (dist(width / 2, height / 2, darts[i+1].x, darts[i+1].y) < width / 2) {
      inCircle++;
      fill(0, 255, 0);
    } else {
      fill(255, 0, 0);
    }
    
    noStroke();
    circle(darts[i+1].x, darts[i+1].y, 5);
  }
  
  pi = 4 * inCircle / darts.length;
  println(pi);
}
class Dart {
 float x, y;
  
 Dart(float i, float j) {
   x = i;
   y = j;
 }
}


  public void settings() { size(800, 800); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "PiCircle" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
